[Unit]
Description=Telemetry backend {{ item.name }}
After=network.target

[Service]
Type=simple
User={{ user }}
Group={{ user }}
ExecStart=/usr/local/bin/telemetry_{{ item.name }} {{ item.args }}
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
